# Command: maintain

## Trigger
Invoked via `/maintain` or `maintain`.

## Actions
1. Scan latest modifications to conventions, architecture, or schemas.
2. Propose corresponding changes to `.agent/project-rules/<project-name>/` rules files.
3. Show the proposed diffs and write them only after explicit `y` gate.
