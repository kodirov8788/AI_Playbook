# Playbook Meta Overview

## Purpose
A central, reusable intelligence layer and operational system for AI-assisted engineering. This repository acts as the single source of truth for agent behavior, skills, commands, and communication styles.

## How it works
Projects link to this central directory (`~/AI_Playbook/`). Agents query `index.md` at runtime to selectively load only the context needed for the active task.
